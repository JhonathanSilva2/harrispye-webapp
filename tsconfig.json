// tsconfig.json (root)
{
    "extends": "./tsconfig.base.json",
    "compilerOptions": { "baseUrl": ".", "noEmit": true },
    "include": ["next-env.d.ts", ".next/types/**/*.ts", "src", "src/types"],
    "exclude": [
        "node_modules",
        "tests",
        "**/*.test.ts",
        "**/*.test.tsx",
        "cypress"
    ]
}
